/* Ecomfy — Inteligência de Conciliação, Financeiro e Decisões */
(function(){
'use strict';
if(!/painel-vendas\.html$/i.test(location.pathname))return;
const money=v=>{if(typeof v==='number')return Number.isFinite(v)?v:0;let s=String(v??'').trim();if(!s)return 0;let neg=/^\s*-|\(.*\)/.test(s),n=parseFloat(s.replace(/[^0-9,.-]/g,'').replace(/\./g,'').replace(',','.'));return Number.isFinite(n)?neg?-Math.abs(n):n:0};
const pct=n=>(Number.isFinite(n)?n:0).toLocaleString('pt-BR',{style:'percent',maximumFractionDigits:1}),brl=n=>(Number.isFinite(n)?n:0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),num=n=>(Number.isFinite(n)?n:0).toLocaleString('pt-BR');
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),norm=s=>String(s??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const rows=id=>{const t=document.querySelector('#'+id+' tbody');return t?[...t.querySelectorAll('tr')]:[]},cells=tr=>[...tr.children].map(x=>x.textContent.trim());
function read(){return{conc:rows('tblConciliacao').map(tr=>{let c=cells(tr);return{id:c[0],date:c[1],status:c[2],gross:money(c[3]),fees:money(c[4]),discounts:money(c[5]),net:money(c[6])}}),products:rows('tblProdutos').map(tr=>{let c=cells(tr);return{sku:c[0],name:c[1],qty:money(c[2]),revenue:money(c[3]),cmv:money(c[4]),margin:money(c[5]),marginPct:money(c[6])/100}}),statuses:rows('tblStatus').map(tr=>{let c=cells(tr);return{status:c[0],count:money(c[1]),pct:money(c[2])/100}}),finance:rows('tblFinanceiro').map(tr=>{let c=cells(tr);return{label:c[0],value:money(c[1]),raw:c[1]}})}}
function financeValue(label,a){let x=a.find(v=>norm(v.label).includes(norm(label)));return x?x.value:0}function kpi(label){let c=[...document.querySelectorAll('#kpis .kpi')].find(x=>(x.querySelector('.label')?.textContent||'').trim()===label);return c?money(c.querySelector('.value')?.textContent):0}
function recs(d){let out=[],p=d.products.filter(x=>x.revenue>0),low=p.filter(x=>x.marginPct<.15).sort((a,b)=>a.marginPct-b.marginPct).slice(0,3);if(low.length)out.push({t:'danger',h:'Revisar preço/custo',s:`${low.map(x=>esc(x.sku||x.name)).join(', ')} estão abaixo de 15% de margem.`});let rt=p.reduce((s,x)=>s+x.revenue,0),top=p.slice().sort((a,b)=>b.revenue-a.revenue).slice(0,3),con=rt?top.reduce((s,x)=>s+x.revenue,0)/rt:0;if(con>=.6)out.push({t:'warn',h:'Receita concentrada',s:`Os 3 principais SKUs representam ${pct(con)} da receita dos produtos.`});let high=p.filter(x=>x.marginPct>=.30).sort((a,b)=>b.margin-a.margin).slice(0,3);if(high.length)out.push({t:'ok',h:'Produtos para escalar',s:`${high.map(x=>esc(x.sku||x.name)).join(', ')} combinam margem de 30%+ com vendas.`});let unpaid=(d.statuses.find(x=>/nao pago/i.test(norm(x.status)))?.count)||0,total=d.statuses.reduce((s,x)=>s+x.count,0);if(total&&unpaid/total>=.05)out.push({t:'warn',h:'Pedidos não pagos',s:`${num(unpaid)} pedido(s) não pago(s) devem ser acompanhados antes de contar como venda realizada.`});return out.slice(0,4)}
function financialUI(box){if(box.querySelector('#financial-recon'))return;let sec=document.createElement('div');sec.id='financial-recon';sec.className='financial-recon';sec.innerHTML=`<div class="financial-head"><div><div class="decision-eyebrow">Conciliação financeira</div><h3>Feche o ciclo: vendido → recebido</h3><p>O Order All mostra a operação. Adicione o relatório de renda/repasse da Shopee para validar o valor efetivamente recebido.</p></div><label class="financial-upload"><input id="financial-file" type="file" accept=".xlsx,.xls,.csv"><span>＋ Importar relatório financeiro</span></label></div><div id="financial-status" class="financial-status"><span class="financial-dot"></span><div><b>Segunda fonte ainda não importada</b><small>Sem ela, frete subsidiado, reembolsos e ajustes podem impedir uma conciliação de caixa definitiva.</small></div></div><div id="financial-result"></div>`;box.appendChild(sec);sec.querySelector('#financial-file').addEventListener('change',financialFile)}
function mapHeaders(rows){let h=rows[0]||[],find=terms=>h.findIndex(v=>terms.some(t=>norm(v).includes(norm(t))));return{h,order:find(['id do pedido','numero do pedido','order id','orderid']),amount:find(['valor liquido','valor recebido','valor da transacao','total global','valor']),credit:find(['credito','credit','entrada']),debit:find(['debito','debit','saida'])}}
function amount(row,m){if(m.credit>=0||m.debit>=0)return(m.credit>=0?money(row[m.credit]):0)-(m.debit>=0?money(row[m.debit]):0);return m.amount>=0?money(row[m.amount]):0}
function financialFile(e){let f=e.target.files?.[0];if(!f)return;let st=document.getElementById('financial-status'),res=document.getElementById('financial-result');st.innerHTML='<span class="financial-dot loading"></span><div><b>Processando relatório…</b><small>O arquivo é analisado no navegador.</small></div>';res.innerHTML='';if(typeof XLSX==='undefined'){st.innerHTML='<span class="financial-dot danger"></span><div><b>Leitor de planilhas indisponível</b></div>';return}let r=new FileReader;r.onload=x=>{try{let wb=XLSX.read(new Uint8Array(x.target.result),{type:'array'}),ws=wb.Sheets[wb.SheetNames[0]],rs=XLSX.utils.sheet_to_json(ws,{header:1,defval:''}).filter(a=>a.some(v=>String(v).trim()!==''));if(rs.length<2)throw Error('arquivo sem linhas de dados');let m=mapHeaders(rs),data=rs.slice(1),d=window.__ecomfyDecisionData||read(),ids=new Set(d.conc.map(x=>norm(x.id)).filter(Boolean)),usable=data.filter(a=>m.amount>=0||m.credit>=0||m.debit>=0),matched=m.order>=0?usable.filter(a=>ids.has(norm(a[m.order]))):[],total=usable.reduce((s,a)=>s+amount(a,m),0),matchTotal=matched.reduce((s,a)=>s+amount(a,m),0),conf=m.order>=0&&(m.amount>=0||m.credit>=0||m.debit>=0),oc=m.order>=0?esc(m.h[m.order]):'não detectada',ac=m.amount>=0?esc(m.h[m.amount]):((m.credit>=0||m.debit>=0)?'crédito/débito':'não detectada');st.innerHTML=`<span class="financial-dot ${conf?'ok':'warn'}"></span><div><b>${conf?'Fonte financeira reconhecida':'Arquivo importado, requer revisão'}</b><small>${esc(f.name)} · ${num(data.length)} movimentos · pedido: ${oc} · valor: ${ac}</small></div>`;res.innerHTML=`<div class="financial-kpis"><div><span>Movimento líquido detectado</span><b>${brl(total)}</b></div><div><span>Pedidos encontrados no Order All</span><b>${num(matched.length)}</b><small>${m.order>=0?brl(matchTotal)+' correspondentes':'coluna de pedido não identificada'}</small></div><div><span>Taxa de correspondência</span><b>${pct(usable.length?matched.length/usable.length:0)}</b><small>pré-conciliação automática</small></div></div><div class="financial-note"><b>Próxima camada:</b> com o layout financeiro confirmado, o Ecomfy poderá fechar por pedido: <strong>valor devido × valor recebido × diferença</strong>, classificando frete, taxas, reembolsos e ajustes.</div>`}catch(err){st.innerHTML=`<span class="financial-dot danger"></span><div><b>Não foi possível processar</b><small>${esc(err.message||'formato inválido')}</small></div>`}};r.readAsArrayBuffer(f)}
function mount(){let dash=document.getElementById('dashboard'),k=document.getElementById('kpis');if(!dash||dash.style.display==='none'||!k?.children.length)return false;let d=read();window.__ecomfyDecisionData=d;let box=document.getElementById('decision-center');if(!box){box=document.createElement('section');box.id='decision-center';box.className='decision-center';let first=document.querySelector('#panel-overview');first?.parentNode?.insertBefore(box,first)}let gross=d.conc.reduce((s,x)=>s+x.gross,0),fees=d.conc.reduce((s,x)=>s+x.fees,0),discounts=d.conc.reduce((s,x)=>s+x.discounts,0),net=d.conc.reduce((s,x)=>s+x.net,0),diff1=gross-kpi('Receita bruta'),diff2=net-financeValue('Renda estimada',d.finance),unpaid=(d.statuses.find(x=>/nao pago/i.test(norm(x.status)))?.count)||0,total=d.statuses.reduce((s,x)=>s+x.count,0),cancelled=d.statuses.filter(x=>/cancel/i.test(norm(x.status))).reduce((s,x)=>s+x.count,0)+unpaid,rate=total?cancelled/total:0,noCost=d.products.filter(x=>/sem custo/i.test(norm(x.cmv))).length,totalP=d.products.length,freightMissing=d.finance.some(x=>/nao disponivel neste relatorio/i.test(norm(x.raw))),alerts=[];if(freightMissing)alerts.push({t:'warn',h:'Frete ainda não conciliável',s:'O Order All não traz o subsídio/desconto de frete; a renda líquida é estimativa.'});if(noCost)alerts.push({t:'warn',h:`${num(noCost)} SKU(s) sem CMV`,s:'A margem desses produtos não representa lucro real.'});if(rate>=.1)alerts.push({t:'danger',h:'Cancelamento + não pagamento acima de 10%',s:`${pct(rate)} dos pedidos estão fora do resultado.`});let neg=d.products.filter(x=>x.margin<0&&!/sem custo/i.test(norm(x.cmv)));if(neg.length)alerts.push({t:'danger',h:`${num(neg.length)} SKU(s) com margem negativa`,s:'Priorize revisão de preço, custo ou campanhas.'});let status=Math.abs(diff1)<.01&&Math.abs(diff2)<.01?'OK':'REVISAR',bridge=[['Receita bruta conciliada',gross,'var(--blue)'],['− Taxas Shopee',fees,'var(--red)'],['− Descontos identificados',discounts,'var(--amber)'],['= Renda estimada pelo relatório',net,'var(--green)']],rr=recs(d);box.innerHTML=`<div class="decision-head"><div><div class="decision-eyebrow">Inteligência financeira</div><h2>Conciliação & decisões</h2><p>Visão executiva do que já fecha, do que está faltando e onde agir primeiro.</p></div><div class="decision-confidence ${freightMissing?'warn':'ok'}"><span></span><div><b>Conciliação ${freightMissing?'PARCIAL':'ALTA'}</b><small>${freightMissing?'Order All sem subsídio de frete':'Campos financeiros disponíveis'}</small></div></div></div><div class="decision-grid"><div class="decision-card"><h3>Reconciliação matemática</h3><div class="bridge">${bridge.map(x=>`<div><span>${x[0]}</span><b style="color:${x[2]}">${brl(x[1])}</b></div>`).join('')}</div><div class="recon-check ${status==='OK'?'ok':'warn'}"><b>${status==='OK'?'✓':'!'} Pedido a pedido</b><span>Diferença: ${brl(Math.max(Math.abs(diff1),Math.abs(diff2)))}</span></div></div><div class="decision-card"><h3>Alertas de controle</h3><div class="alert-list">${(alerts.length?alerts:[{t:'ok',h:'Nenhum alerta crítico detectado',s:'Dentro dos limites que este relatório permite avaliar.'}]).map(a=>`<div class="decision-alert ${a.t}"><b>${esc(a.h)}</b><span>${esc(a.s)}</span></div>`).join('')}</div></div></div><div class="decision-grid decision-grid-3"><div class="decision-mini"><span>Pedidos analisados</span><b>${num(total)}</b><small>${num(unpaid)} não pagos</small></div><div class="decision-mini"><span>Cancelamento + não pagamento</span><b>${pct(rate)}</b><small>${num(cancelled)} fora do resultado</small></div><div class="decision-mini"><span>SKUs com CMV preenchido</span><b>${num(Math.max(0,totalP-noCost))}/${num(totalP)}</b><small>${noCost?num(noCost)+' sem custo':'cobertura completa'}</small></div></div><div class="decision-card recommendations"><h3>Decisões recomendadas</h3><div class="recommendation-list">${(rr.length?rr:[{t:'ok',h:'Base saudável',s:'Continue acompanhando margem, concentração e custos nos próximos períodos.'}]).map(a=>`<div class="decision-alert ${a.t}"><b>${a.h}</b><span>${a.s}</span></div>`).join('')}</div></div><div class="decision-footer"><b>Regra de ouro:</b> Order All = operação. Relatório de renda/repasse = caixa. Só consideraremos uma conciliação de <strong>100%</strong> quando as duas fontes fecharem por pedido.</div>`;financialUI(box);return true}
const style=document.createElement('style');style.textContent=`.decision-center{margin:0 0 22px}.decision-head{background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:20px;display:flex;justify-content:space-between;gap:20px;align-items:flex-start}.decision-eyebrow{font-size:10px;text-transform:uppercase;letter-spacing:.1em;color:var(--blue);font-weight:800;margin-bottom:5px}.decision-head h2{margin:0;font-size:18px}.decision-head p{margin:5px 0 0;color:var(--text-dim);font-size:12px;max-width:760px;line-height:1.5}.decision-confidence{display:flex;gap:9px;align-items:center;padding:9px 12px;border:1px solid var(--border);border-radius:10px;min-width:180px}.decision-confidence>span{width:9px;height:9px;border-radius:50%;background:var(--green)}.decision-confidence.warn>span{background:var(--amber)}.decision-confidence b{display:block;font-size:12px}.decision-confidence small{display:block;color:var(--text-faint);font-size:10px;margin-top:2px}.decision-grid{display:grid;grid-template-columns:1.1fr 1fr;gap:18px;margin-top:18px}.decision-grid-3{grid-template-columns:repeat(3,1fr)}.decision-card,.decision-mini{background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:18px}.decision-card h3{margin:0 0 14px;font-size:13px}.bridge>div{display:flex;justify-content:space-between;gap:15px;padding:8px 0;border-bottom:1px solid var(--border);font-size:12px}.bridge>div:last-child{border-bottom:0}.bridge span{color:var(--text-dim)}.bridge b{font-family:'IBM Plex Mono',monospace}.recon-check{margin-top:12px;padding:9px 11px;border-radius:8px;display:flex;justify-content:space-between;gap:10px;font-size:11px;background:var(--green-light);color:var(--green)}.recon-check.warn{background:var(--amber-light);color:var(--amber)}.recon-check span{color:var(--text-dim)}.alert-list,.recommendation-list{display:grid;gap:8px}.decision-alert{padding:10px 11px;border-radius:8px;display:grid;gap:3px;background:var(--bg);border-left:3px solid var(--blue)}.decision-alert.warn{border-left-color:var(--amber)}.decision-alert.danger{border-left-color:var(--red)}.decision-alert.ok{border-left-color:var(--green)}.decision-alert b{font-size:11.5px}.decision-alert span{font-size:11px;line-height:1.45;color:var(--text-dim)}.decision-mini span{display:block;color:var(--text-dim);font-size:11px}.decision-mini b{display:block;font-family:'IBM Plex Mono',monospace;font-size:20px;margin:5px 0 2px}.decision-mini small{color:var(--text-faint);font-size:10.5px}.recommendations{margin-top:18px}.decision-footer{margin-top:12px;padding:12px 14px;border:1px dashed var(--border);border-radius:8px;color:var(--text-dim);font-size:11px;line-height:1.5}.decision-footer b{color:var(--text)}.financial-recon{margin-top:18px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:18px}.financial-head{display:flex;justify-content:space-between;gap:18px;align-items:center}.financial-head h3{margin:0;font-size:14px}.financial-head p{margin:4px 0 0;color:var(--text-dim);font-size:11.5px;line-height:1.45;max-width:760px}.financial-upload{display:inline-flex;align-items:center;justify-content:center;white-space:nowrap;padding:9px 13px;border:1px solid var(--border);border-radius:7px;background:var(--bg);color:var(--text);font-size:11.5px;font-weight:700;cursor:pointer}.financial-upload:hover{border-color:var(--blue);color:var(--blue)}.financial-upload input{display:none}.financial-status{margin-top:14px;padding:11px 12px;background:var(--bg);border-radius:8px;display:flex;gap:9px;align-items:flex-start}.financial-dot{width:9px;height:9px;border-radius:50%;background:var(--amber);margin-top:3px;flex:0 0 auto}.financial-dot.ok{background:var(--green)}.financial-dot.danger{background:var(--red)}.financial-dot.loading{animation:ecpulse 1s infinite}.financial-status b{display:block;font-size:11.5px}.financial-status small{display:block;color:var(--text-faint);font-size:10.5px;line-height:1.4;margin-top:2px}.financial-kpis{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:12px}.financial-kpis>div{padding:12px;background:var(--bg);border:1px solid var(--border);border-radius:8px}.financial-kpis span{display:block;color:var(--text-dim);font-size:10.5px}.financial-kpis b{display:block;font-family:'IBM Plex Mono',monospace;font-size:17px;margin:5px 0 2px}.financial-kpis small{color:var(--text-faint);font-size:9.5px}.financial-note{margin-top:10px;padding:10px 12px;border-left:3px solid var(--blue);background:var(--blue-light);color:var(--text-dim);font-size:10.5px;line-height:1.5}.financial-note strong{color:var(--text)}@keyframes ecpulse{50%{opacity:.35}}@media(max-width:900px){.decision-head,.decision-grid{grid-template-columns:1fr;display:grid}.decision-confidence{min-width:0}.decision-grid-3,.financial-kpis{grid-template-columns:1fr}.financial-head{display:grid}.financial-upload{width:100%}}`;document.head.appendChild(style);
let timer=null,lastSig='';function sig(){let d=read();return JSON.stringify([d.conc.length,d.products.length,d.statuses.map(x=>[x.status,x.count]),d.finance.map(x=>x.raw),d.products.map(x=>[x.sku,x.revenue,x.cmv,x.margin])])}function schedule(){clearTimeout(timer);timer=setTimeout(()=>{let s=sig();if(s!==lastSig){lastSig=s;mount()}},120)}function init(){let dash=document.getElementById('dashboard');if(!dash)return;let obs=new MutationObserver(ms=>{if(ms.some(m=>{let t=m.target;return !(t.closest&&t.closest('#decision-center'))}))schedule()});obs.observe(dash,{subtree:true,childList:true,attributes:true,attributeFilter:['style']});schedule()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
